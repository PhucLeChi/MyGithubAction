name: 'Hello World'
description: 'Greet someone'

outputs:
  build_number:
    description: "Random number"
    value: ${{ steps.build_number.outputs.myMask }}

runs:
  using: "composite"
  steps:
    - name: Set variables
      id: build_number
      shell: pwsh
      run: |
        echo "Set available as mask"
        echo "buildNumber=$(date +'%Y.%m.%d').${{ github.run_number }}"
        $myMask=$(date +'%Y.%m.%d').${{ github.run_number }}
        echo "$myMask"
        echo "::add-mask::$myMask"
        echo "myMask=$myMask" >> $env:GITHUB_OUTPUT
